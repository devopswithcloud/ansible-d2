## Install/COnfigure nexus using Ansible 
* Create a VM using Terraform 
* Get the VM Ip using ansible
* Configure ansible on the controller 
* Make the IP entry into the host file, 
* Create a playbook to install `Nexus` 